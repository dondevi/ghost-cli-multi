<nav role="navigation">
  {{#if prev}}
    <a href="{{page_url prev}}">上一页</a> |
  {{/if}}
  <span>第 {{page}} 页</span>
  {{#if next}}
     <a href="{{page_url next}}">下一页</a>
  {{/if}}
  <span>共 {{pages}} 页, 共 {{total}} 条</span>
</nav>
